package algo;

import maps.MapConstants;
import maps.Map;
import robot.RobotConstants;

public class HelperFunctions {
	private Map map;
	/**
	 * Find the cost when robot moves from A to B
	 * @param a row, column and current direction of A
	 * @param b row, column and desired direction of B
	 * @return traveling cost
	 */
	
}
